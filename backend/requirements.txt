fastapi
uvicorn
pydantic
joblib
# Removed numpy/sklearn to test if build passes without them (debugging timeout)
# If this passes build but fails runtime, we know the issue is definitely compiling these libs.
nltk
textblob
beautifulsoup4
requests
python-dotenv
supabase
